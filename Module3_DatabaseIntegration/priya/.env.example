# Database Configuration
# DO NOT commit actual credentials - this is a template only
DB_DRIVER=mysql+mysqlconnector
DB_USER=root
DB_PASSWORD=your_actual_password_here
DB_HOST=localhost
DB_PORT=3306
DB_NAME=college_db_orm
DB_ECHO=False

# Create a .env file with your actual credentials and add it to .gitignore
